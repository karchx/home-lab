auth:
  username: platform
  password: "changeme123"
  database: platform

primary:
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"

  persistence:
    enabled: true
    size: 200Gi
    storageClass: "local-path"

  readReplicas:
    replicaCount: 0

  metrics:
    enabled: false
